import { Box } from '@mui/material';
import React from 'react';

const ModalMenu = () => {
    return (
        <Box px={2} mt={2}>
            <h1>Menu </h1>
        </Box>
    );
};

export default ModalMenu;
